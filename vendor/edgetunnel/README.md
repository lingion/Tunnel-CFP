# edgetunnel vendor

VLESS / Reality / XTLS Vision Flow / gRPC / ECH / uTLS Worker 实现。

支持协议：VLESS over WebSocket + TLS（Reality + XTLS Vision Flow），gRPC transport，ECH Encrypted Client Hello，uTLS fingerprinting。

命名约定：常量/变量使用中文（CamelCase），反映 b3d1fb3 命名补丁。