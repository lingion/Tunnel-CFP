{
  "name": "tunnel-cfp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "@cloudflare/workers-types": "^4.20250903.0",
    "@types/dns-packet": "^5.6.5",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "dns-packet": "^5.6.1",
    "js-yaml": "^5.4.1"
  },
  "description": "Single Cloudflare Worker: VLESS tunnel + subscription manager + DoH + web proxy + WS bridge",
  "license": "GPL-2.0"
}
